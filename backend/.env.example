# Copy to .env and fill in. docker-compose auto-loads backend/.env.
# Cloudflare Tunnel connector token (Zero Trust dashboard -> Networks -> Tunnels
# -> create/select a tunnel -> install connector -> copy the token).
# Used by the `cloudflared` service (profile: tunnel).
CLOUDFLARE_TUNNEL_TOKEN=
